\section{问题四：混合干扰源的自动检测与清除}

\subsection{问题分析}

定向源只在其发射方向两侧各 \(90\degree\) 的范围内可见，单次无信号不能证明频道不存在。策略需增加多方位复测和空间覆盖机制，以降低方向性遮蔽造成的漏检风险。

\subsection{模型建立：定向源检测与覆盖补偿}

将频道状态扩展为
\[
  z_c=(n_c^{+},\mathcal{P}_c^{+},\mathcal{P}_c^{-},\Omega_c,q_c),
\]
其中 \(\mathcal{P}_c^{+}\) 和 \(\mathcal{P}_c^{-}\) 分别记录有信号与无信号的检测点，\(q_c\) 为清除状态。对未知频道，不以单点无信号作排除判断；只有当检测点从多个方位围绕目标区域且覆盖距离满足要求时，才降低该频道的搜索优先级。

策略在问题三基础上增加三项机制：
\begin{enumerate}
  \item \textbf{多方向进入：}覆盖路线包含方向差异较大的径向与环向航段；
  \item \textbf{背面补测：}某频道首次出现后，在估计源位置的不同方位安排检测点，避免始终处于辐射背面；
  \item \textbf{失败恢复：}接近估计位置仍无信号时，不立即放弃，而是沿安全环绕点检测并尝试进入 \(20\,\mathrm{m}\) 光学范围。
\end{enumerate}

可定义频道--网格单元的覆盖置信度
\begin{equation}
  Q_{c,g}=1-\prod_{p\in\mathcal{P}_{c,g}}
  \bigl(1-\eta_d(p,g)\eta_a(p,g)\bigr),
\end{equation}
其中 \(\eta_d\) 表示距离可探测性，\(\eta_a\) 表示方位多样性。\placeholder{给出实际使用的置信度近似与阈值。}

\subsection{模型求解：算法设计}

\begin{modelalgorithm}{全向与定向干扰源统一检测清除}
  \label{alg:p4}
  \AlgoIn{目标圆域、20 个频道、方向覆盖参数}
  \AlgoOut{清除记录、频道观测状态与行为日志}
  \begin{enumerate}[leftmargin=2.5em]
    \AlgoStep{初始化多方向覆盖航点与频道状态。}
    \AlgoStep{计算可清除目标、待复测目标和低置信覆盖单元的收益/时间比。}
    \AlgoStep{选择收益/时间比最大的安全动作，执行移动、换台与检测。}
    \AlgoStep{分类记录观测；若获得示向度，则更新定位区域并生成异方位复测点。}
    \AlgoStep{进入光学范围时清除；接近失败时转入有次数上限的环绕补测。}
    \AlgoStep{满足终止条件后主动退出并保存原始日志。}
  \end{enumerate}
\end{modelalgorithm}

为避免定向源反复补测耗尽时间，对每个频道设置补测预算，并在剩余程序时间较少时优先清除定位置信度最高的目标。\placeholder{填写预算、收益函数和终止阈值。}

\subsection{结果分析：演练与正式测试结果}

沿用问题三的评价指标：
\begin{equation}
  \text{清除比例}=\frac{N_c}{N},\qquad
  \text{平均定位清除时间}=\frac{T_{\mathrm{all}}}{N_c}.
\end{equation}

\begin{table}[H]
  \centering
  \caption{问题四演练测试统计}
  \scriptsize
  \begin{tabularx}{\textwidth}{c*{5}{>{\centering\arraybackslash}X}}
    \toprule
    演练次数 & 平均清除比例 & 平均时间/s & 平均补测次数 & 最差清除比例 & 程序时间/s \\
    \midrule
    \tableplaceholder & \tableplaceholder & \tableplaceholder & \tableplaceholder & \tableplaceholder & \tableplaceholder \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{问题四正式测试结果}
  \label{tab:p4-formal}
  \small
  \begin{tabularx}{\textwidth}{>{\centering\arraybackslash}X*{3}{>{\centering\arraybackslash}X}}
    \toprule
    测试案例编码 & 清除干扰源个数 & 平均定位清除时间/s & 程序运行时间/s \\
    \midrule
    \tableplaceholder & \tableplaceholder & \tableplaceholder & \tableplaceholder \\
    \tableplaceholder & \tableplaceholder & \tableplaceholder & \tableplaceholder \\
    \tableplaceholder & \tableplaceholder & \tableplaceholder & \tableplaceholder \\
    \bottomrule
  \end{tabularx}
\end{table}

\placeholder{对比问题三与问题四策略，重点分析定向源漏检率及补测的时间代价。}

\subsection{本问小结}

本问在问题三状态机基础上增加方向覆盖、背面补测和失败恢复机制，用以处理定向源带来的观测不完整性。后续结果分析将集中比较补测带来的清除率提升与额外时间代价。
