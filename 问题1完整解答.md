# B 题问题一完整解答

## 1. 问题重述

在第 (i) 个检测点 (S_i=(x_i,y_i)) 处，测向机给出同一干扰源的示向度 α_i。由于测量误差不超过 (1^°)，干扰源的真实方向应落在

\[
[\alpha_i-1^\circ,\alpha_i+1^\circ]
\]

所确定的扇形内。多个检测点的扇形公共部分形成多边形定位区域。问题要求：

1. 计算定位区域内任意两点最大距离，即定位区域直径；
2. 判断以该直径为直径所作的圆能否覆盖整个定位区域。

## 2. 模型假设

1. 检测点坐标和示向度均已知，坐标单位为米，角度单位为度；
2. 每次示向度误差均在 ±1° 内；
3. 交会形成非空、有界且面积大于零的定位区域；若条件不足，程序会报告无界或退化，需要增加检测点；
4. 计算中忽略地球曲率和设备高度差，采用二维平面几何模型。

## 3. 示向度约束的建立

记第 (i) 次测量的下、上边界角为

\[
\theta_i^- = \alpha_i-\varepsilon,\qquad
\theta_i^+ = \alpha_i+\varepsilon,\qquad \varepsilon=1^\circ.
\]

相应的单位方向向量为

\[
\boldsymbol u_i^-=(\cos\theta_i^-,\sin\theta_i^-),\qquad
\boldsymbol u_i^+=(\cos\theta_i^+,\sin\theta_i^+).
\]

设干扰源的可能位置为 (P=(x,y))。为使向量 (P-S_i) 位于两条边界射线之间，需要满足二维叉积条件

\[
\boldsymbol u_i^-\times(P-S_i)\ge 0,
\qquad
\boldsymbol u_i^+\times(P-S_i)\le 0.
\]

展开后得到两个线性半平面：

\[
\sin\theta_i^-x-\cos\theta_i^-y
\le
\sin\theta_i^-x_i-\cos\theta_i^-y_i,
\]

\[
-\sin\theta_i^+x+\cos\theta_i^+y
\le
-\sin\theta_i^+x_i+\cos\theta_i^+y_i.
\]

因此，(n) 个检测点共给出 (2n) 个半平面。它们的交集为

\[
\Omega=\bigcap_{i=1}^{n}\Omega_i.
\]

因为半平面的交集一定是凸集，所以只要 Ω 有界且非退化，它就是一个凸多边形。

## 4. 定位多边形的计算

采用便于本科竞赛实现的“边界交点枚举法”。设半平面总数为 (m=2n)：

1. 枚举任意两条半平面的边界直线；
2. 若两直线不平行，计算交点；
3. 将交点代入全部 (m) 个不等式，只保留满足所有约束的点；
4. 去除数值上重复的点；
5. 用单调链凸包算法将顶点按逆时针排列。

边界直线

\[
a_1x+b_1y=c_1,\qquad a_2x+b_2y=c_2
\]

的行列式为 Δ=a_1b_2-a_2b_1。当 ∣Δ∣ 大于数值容差时，交点为

\[
x=\frac{c_1b_2-c_2b_1}{\Delta},\qquad
y=\frac{a_1c_2-a_2c_1}{\Delta}.
\]

枚举产生 (O(m^2)) 个候选交点，每个点需检查 (m) 个约束，故该部分复杂度为 (O(m^3))。本题检测点数通常很少，这种写法简单、稳定，运行量完全可以接受。

## 5. 多边形直径

设定位多边形的逆时针顶点为

\[
V_0,V_1,\ldots,V_{k-1}.
\]

凸多边形内的最远点对一定可以在顶点中找到，因此定位区域直径为

\[
D=\max_{0\le i<j<k}\|V_i-V_j\|_2.
\]

顶点较少时可直接两两枚举，复杂度为 (O(k^2))。正式算法采用旋转卡壳：沿多边形各边移动，同时维护与当前边距离最大的对踵点，扫描一周即可得到最远顶点对，复杂度为 (O(k))。程序中同时保留 (O(k^2)) 方法，用来校验旋转卡壳的结果。

### 算法伪代码

```text
输入：检测点 S_i、示向度 α_i，i=1,...,n
输出：定位多边形、直径 D、直径端点 A 和 B

H = 空列表
for 每个观测 (S_i, α_i):
    将 α_i-1° 与 α_i+1° 转为两个半平面，加入 H

C = 空列表
for H 中每一对边界直线:
    if 两直线不平行:
        P = 两直线交点
        if P 满足 H 中全部不等式:
            将 P 加入 C

V = ConvexHull(去重后的 C)
若 V 无界、为空或少于 3 个顶点，则报告无法形成有效定位多边形
(D, A, B) = RotatingCalipers(V)
返回 V、D、A、B
```

## 6. 直径圆能否覆盖定位区域

取一组直径端点 (A,B)，作圆心和半径

\[
O=\frac{A+B}{2},\qquad R=\frac{D}{2}.
\]

### 6.1 一般结论

**该圆不一定能覆盖定位区域。**

例如，取边长为 2 的等边三角形。它的直径等于最长边长，即 (D=2)。若以其中一条边为直径作圆，则 (R=1)，而第三个顶点到圆心的距离为 √3，大于 1，因此第三个顶点在圆外。

所以，“圆的直径等于区域直径”只说明圆的尺度相同，并不能保证圆的位置和形状足以覆盖整个区域。

### 6.2 对具体定位区域的判定

圆盘和定位多边形都是凸集。只要定位多边形的全部顶点都位于圆内，所有边及内部点也必定位于圆内。因此只需检查

\[
\|V_i-O\|_2\le R,\qquad i=0,1,\ldots,k-1.
\]

若全部成立，则该直径圆可以覆盖当前定位区域；否则不能。若存在多组并列的直径端点，可分别检查相应的圆。

## 7. Python 程序

完整程序位于 [`src/problem1.py`](src/problem1.py)，测试位于 [`tests/test_problem1.py`](tests/test_problem1.py)。程序只使用 Python 标准库，主要接口如下：

```python
from src.problem1 import solve

observations = [
    # ((检测点 x, 检测点 y), 示向度, 误差上限)
    ((-500.0, -200.0), 37.46989764584402, 1.0),
    ((900.0, -100.0), 139.3944289077348, 1.0),
    ((0.0, 1000.0), 296.96505117707795, 1.0),
]

result = solve(observations)
print(result["polygon"])
print(result["diameter"])
print(result["diameter_endpoints"])
print(result["circle"])
```

每条观测也可以只写为 `((x, y), 示向度)`，此时程序自动采用题目给定的 1° 误差。

运行完整示例：

```powershell
python -m src.problem1
```

## 8. 算例与结果

为便于复现，构造真实位置为 ((300,400)) 的干扰源，在三个检测点进行测向，并人为加入不超过 1° 的固定误差。输入即为上一节代码中的三条观测。

程序得到的定位多边形顶点为：

| 顶点 | (x) | (y) |
|---|---:|---:|
| (V_1) | 281.8447 | 421.2370 |
| (V_2) | 289.7997 | 404.9015 |
| (V_3) | 303.2828 | 393.7451 |
| (V_4) | 316.6367 | 403.6157 |
| (V_5) | 302.1513 | 430.8989 |
| (V_6) | 298.3041 | 434.3152 |

计算结果为：

| 指标 | 结果 |
|---|---:|
| 定位区域直径 (D) | 40.8745 m |
| 直径端点 | (303.2828, 393.7451)、(298.3041, 434.3152) |
| 直径圆圆心 | (300.7934, 414.0302) |
| 直径圆半径 | 20.4372 m |
| 本算例是否覆盖 | 是 |

真实位置 ((300,400)) 满足全部测向半平面约束，说明构造出的定位区域与设定相符。需要注意，本算例中的直径圆可以覆盖定位区域，但这不改变第 6 节的总体结论：对一般定位多边形不能保证覆盖。

## 9. 程序验证

在 Python 3.13.1、pytest 9.0.1 环境中运行：

```powershell
python -m pytest -v
```

自动化测试共 12 项，结果全部通过：

| 测试内容 | 验证目的 | 结果 |
|---|---|---|
| 正东方向与反方向点 | 半平面朝向正确 | 通过 |
| 359.5° 跨越 0° | 角度周期处理正确 | 通过 |
| 非法误差范围 | 输入检查有效 | 通过 |
| 四组测向形成已知菱形 | 定位多边形顶点正确 | 通过 |
| 含误差的三站测向 | 真实干扰源满足全部约束 | 通过 |
| 单次测向 | 正确识别无界区域 | 通过 |
| 3×4 矩形 | 直径等于对角线 5 | 通过 |
| 等边三角形 | 验证直径圆不一定覆盖 | 通过 |
| 矩形直径圆 | 验证可覆盖情形 | 通过 |
| 22 组随机凸多边形 | 旋转卡壳与暴力枚举一致 | 通过 |
| 少于两个顶点 | 异常输入处理正确 | 通过 |
| 统一 `solve` 接口 | 端到端结果字段完整 | 通过 |

同时运行语法检查：

```powershell
python -m compileall -q src tests
```

若命令无输出且退出码为 0，表示全部 Python 文件均可正常编译。

## 10. 结论

本题可以转化为二维半平面交问题。将每个示向度的 ±1° 误差表示为两个半平面，求所有半平面的公共区域即可得到凸多边形定位区域；随后用旋转卡壳求最远顶点对及定位区域直径。

以定位区域直径为直径所作的圆**一般不能保证覆盖**定位区域，等边三角形给出了直接反例。对具体区域，应以直径端点中点为圆心、直径一半为半径，逐一检查全部多边形顶点后再作判断。
